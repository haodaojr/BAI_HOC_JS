# 🚀 LỘ TRÌNH HỌC REACT ULTIMATE 2025-2027
**Phiên bản tối ưu nhất - Kết hợp Genspark + Claude + Loại bỏ tất cả điểm yếu**

---

## 🎯 TỔNG QUAN ROADMAP

### **Mục tiêu cuối khóa (24 tháng)**
- ✅ Master React 18/19 + Next.js 14+ (Server Components)
- ✅ TypeScript, Testing (80%+ coverage), Performance Optimization
- ✅ Full-stack development với Next.js + Database
- ✅ Portfolio 20+ projects chất lượng cao
- ✅ Sẵn sàng cho vị trí Senior React Developer (150K-200K/year)
- ✅ Có khả năng lead team và mentor juniors

### **Triết lý học tập**
1. **Foundation First** - JavaScript vững = React dễ
2. **Practice > Theory** - Mỗi concept = 1 project
3. **Progressive Complexity** - Từ đơn giản → nâng cao từng tuần
4. **Build Real Products** - Không làm toy apps
5. **Learn in Public** - Share code, blog, networking
6. **Quality > Quantity** - Hiểu sâu > học nhiều

### **Cam kết thời gian**
- **Minimum**: 15-20 giờ/tuần (có thể làm part-time)
- **Recommended**: 25-30 giờ/tuần (full-time learning)
- **Intensive**: 40+ giờ/tuần (career switcher)

---

## 📚 PHASE 1: SOLID FOUNDATION (Tháng 1-6)

> **Mục tiêu**: Làm chủ JavaScript + React fundamentals + Build 8 projects

---

### **THÁNG 1-2: JavaScript Mastery + Web Fundamentals**

#### **Week 1-2: HTML5 & CSS3 Modern**
- **Semantic HTML5**
  - Header, Nav, Main, Section, Article, Footer
  - Form elements (input types, validation)
  - Accessibility attributes (ARIA)
  - SEO-friendly structure

- **CSS3 Advanced**
  - Flexbox (justify-content, align-items, flex-direction)
  - Grid (grid-template, areas, auto-fit/fill)
  - CSS Variables (theming system)
  - Transitions & Animations
  - Media Queries (breakpoints)
  - BEM Methodology (Block, Element, Modifier)

- **Responsive Design**
  - Mobile-first approach
  - Fluid typography
  - Responsive images
  - CSS Grid layouts

**📌 Mini Projects:**
- Landing page responsive
- CSS Grid photo gallery
- Animated button library

---

#### **Week 3-4: JavaScript Core Fundamentals**

**Variables & Data Types:**
- let, const, var (scope differences)
- Primitive vs Reference types
- Type coercion & conversion
- null vs undefined

**Functions:**
- Function declarations vs expressions
- Arrow functions
- IIFE (Immediately Invoked)
- Higher-order functions
- Callback functions
- Default parameters
- Rest parameters

**Objects & Arrays:**
- Object creation & manipulation
- Array methods: forEach, map, filter, reduce
- find, findIndex, some, every
- sort, reverse, splice
- Object destructuring
- Array destructuring
- Spread operator
- Object.keys(), values(), entries()

**📌 Practice:**
- Array manipulation exercises (30+)
- Object transformation problems
- Function composition

---

#### **Week 5-6: ES6+ & Advanced Concepts**

**Asynchronous JavaScript:**
- Callbacks & Callback Hell
- Promises (then, catch, finally)
- Promise.all, Promise.race, Promise.allSettled
- Async/Await syntax
- Error handling (try-catch)
- Fetch API

**Advanced Concepts:**
- Closures (practical examples)
- Hoisting (var, let, const, functions)
- this keyword (all contexts)
- Call, Apply, Bind
- Prototypes & Inheritance
- Classes & Constructor functions

**Modules & OOP:**
- ES6 Modules (import/export)
- Named vs Default exports
- Dynamic imports
- Class syntax
- Inheritance (extends, super)
- Static methods
- Private fields (#)

---

#### **Week 7-8: DOM & Browser APIs**

**DOM Manipulation:**
- querySelector, querySelectorAll
- getElementById, getElementsByClassName
- createElement, appendChild
- innerHTML vs textContent vs innerText
- setAttribute, getAttribute
- classList (add, remove, toggle)

**Events:**
- addEventListener
- Event types (click, input, submit, keydown)
- Event object (target, preventDefault)
- Event bubbling & capturing
- Event delegation (efficient pattern)

**Browser APIs:**
- LocalStorage & SessionStorage
- JSON.parse(), JSON.stringify()
- setTimeout, setInterval, clearTimeout
- Window object (location, history)
- Console methods (log, table, group)

**📦 Projects Tháng 1-2 (Build from scratch):**
1. **Personal Portfolio** - HTML/CSS/JS (responsive, animated)
2. **Todo App** - CRUD với LocalStorage
3. **Weather App** - Fetch API, async/await
4. **Calculator** - Complex logic, event handling
5. **Quiz App** - DOM manipulation, scoring system

**🎯 Checkpoint Tháng 2:**
- [ ] Hiểu tất cả ES6+ syntax
- [ ] Thành thạo array methods
- [ ] Có thể fetch & display API data
- [ ] 5 projects hoàn chỉnh
- [ ] GitHub có 60+ commits

---

### **THÁNG 3-4: React Fundamentals + First Components**

#### **Week 9-10: React Core Concepts**

**Setup & Tooling:**
- Node.js & npm/yarn/pnpm
- **Vite** vs Create React App (recommend Vite)
- VS Code extensions:
  - ES7+ React/Redux/React-Native snippets
  - Prettier, ESLint
  - Auto Rename Tag
  - Bracket Pair Colorizer
- React Developer Tools (Chrome/Firefox)
- Git workflow cho React projects

**JSX Deep Dive:**
- JSX syntax rules
- JSX vs HTML differences
- JavaScript expressions in JSX
- JSX fragments (<> </>)
- Conditional rendering (&&, ternary, if-else)
- Style in JSX (inline, className)
- Comments in JSX

**Components Fundamentals:**
- Functional Components (modern standard)
- ~~Class Components~~ (skip - outdated)
- Component naming conventions
- File structure best practices
- Component composition
- Reusable components

---

#### **Week 11-12: Props, State & Events**

**Props Mastery:**
- Passing props (primitive, objects, arrays)
- Props destructuring
- Children props
- Default props
- **PropTypes** (type checking before TypeScript)
  ```javascript
  import PropTypes from 'prop-types';
  Component.propTypes = {
    name: PropTypes.string.isRequired,
    age: PropTypes.number
  };
  ```
- Props drilling problem

**State with useState:**
- What is state?
- useState Hook syntax
- State updates (immutability)
- Functional updates (prevState)
- Multiple states
- Object state updates
- Array state updates
- State vs Props

**Event Handling:**
- onClick, onChange, onSubmit
- Synthetic Events
- Event object (e.target, e.preventDefault)
- Passing arguments to event handlers
- Event handler naming conventions

**Lists & Keys:**
- Rendering arrays với map()
- Keys importance (unique, stable)
- Index as key (anti-pattern)
- Filtering lists
- Sorting lists

**Forms in React:**
- Controlled components
- Handling inputs (text, number, checkbox, radio, select)
- Multiple inputs (name attribute)
- Form submission
- Basic validation
- Resetting forms

**📦 Projects Tháng 3-4:**
1. **Todo List Advanced** - Add/Edit/Delete/Filter/LocalStorage
2. **Recipe Finder** - Search API, card components
3. **Quiz App React** - Multiple questions, score tracking
4. **Expense Tracker** - Forms, calculations, charts
5. **Movie Search** - OMDB API, debouncing
6. **Shopping Cart UI** - Add/remove items, quantity

**🎯 Checkpoint Tháng 4:**
- [ ] Hiểu Virtual DOM
- [ ] Thành thạo useState
- [ ] Props & PropTypes
- [ ] Controlled forms
- [ ] 6 React projects
- [ ] Component thinking mindset

---

### **THÁNG 5-6: React Hooks Mastery + Custom Hooks**

#### **Week 13-14: Core Hooks Deep Dive**

**useEffect Mastery:**
- Effect timing (after render)
- Dependencies array []
  - Empty array: mount only
  - No array: every render
  - With deps: when deps change
- Cleanup functions (return)
- Multiple effects pattern
- Effect execution order
- Common pitfalls:
  - Infinite loops
  - Missing dependencies
  - Stale closures

**useRef Complete:**
- Accessing DOM elements
- Mutable values (persist across renders)
- Previous value pattern
- Focus management
- Video/audio control
- useRef vs useState

**useContext:**
- Context API introduction
- Creating context
- Provider pattern
- useContext Hook
- Avoiding prop drilling
- Multiple contexts
- Context performance considerations

---

#### **Week 15-16: Performance & Advanced Hooks**

**useMemo:**
- Expensive calculations caching
- When to use (profile first)
- Dependencies array
- Referential equality
- Anti-patterns (premature optimization)

**useCallback:**
- Function memoization
- Preventing re-renders
- Dependencies array
- With useEffect
- Event handlers optimization

**useReducer:**
- When useState isn't enough
- Reducer function (state, action)
- Dispatch actions
- Action types pattern
- Complex state logic
- useReducer vs useState
- Context + Reducer pattern

**Custom Hooks:**
- Creating reusable logic
- Naming convention (use...)
- Extracting component logic
- Testing custom hooks
- Popular patterns:
  - useLocalStorage
  - useFetch
  - useDebounce
  - useToggle
  - useWindowSize

**Rules of Hooks:**
- Only call at top level
- Only call in React functions
- ESLint plugin (eslint-plugin-react-hooks)

**📦 Projects Tháng 5-6:**
1. **E-commerce Cart** - useReducer, Context
2. **Dark Mode App** - useContext, localStorage
3. **Infinite Scroll Gallery** - useEffect, useRef, IntersectionObserver
4. **Multi-step Form Wizard** - useState, validation
5. **Custom Hooks Library** - useDebounce, useLocalStorage, useFetch
6. **Real-time Search** - debouncing, API calls

**🎯 Checkpoint Tháng 6:**
- [ ] Master tất cả React Hooks
- [ ] Hiểu useEffect dependencies
- [ ] Tạo được custom hooks
- [ ] Context API patterns
- [ ] 12 React projects total
- [ ] Code clean, có structure

---

## 🎨 PHASE 2: STYLING, ROUTING & STATE (Tháng 7-12)

> **Mục tiêu**: UI Development + Navigation + Global State Management

---

### **THÁNG 7-8: Styling & UI Component Development**

#### **Week 17-18: CSS-in-React Approaches**

**CSS Modules:**
- Scoped CSS
- Naming conventions
- Composing styles
- Global vs Local
- CSS Modules with Vite/CRA

**CSS-in-JS (Styled Components):**
```javascript
import styled from 'styled-components';

const Button = styled.button`
  background: ${props => props.primary ? 'blue' : 'gray'};
  &:hover { opacity: 0.8; }
`;
```
- Tagged templates
- Props-based styling
- Theming (ThemeProvider)
- Global styles
- Animations

**Tailwind CSS** ⭐ (Recommended):
- Utility-first philosophy
- JIT (Just-In-Time) compiler
- Configuration (tailwind.config.js)
- Custom classes (@apply)
- Responsive design (sm:, md:, lg:)
- Dark mode (dark:)
- Arbitrary values [color:#123456]
- Plugins ecosystem

---

#### **Week 19-20: UI Libraries & Design Systems**

**Material-UI (MUI):**
- Installation & setup
- Core components
- Theming & customization
- sx prop
- Grid system
- Form components

**Ant Design:**
- Enterprise-grade components
- Form handling
- Data display (Table, List)
- Feedback (Message, Notification)
- Layout components

**Shadcn/ui** (Modern approach):
- Copy-paste components
- Radix UI primitives
- Full control & customization
- TypeScript support
- Tailwind CSS based

**Headless UI:**
- Unstyled components
- Full styling control
- Accessibility built-in

**Responsive Design Patterns:**
- Mobile-first approach
- Breakpoint strategies
- Container queries
- Fluid typography
- Responsive images

**Accessibility (a11y):**
- ARIA attributes
- Semantic HTML
- Keyboard navigation
- Screen reader testing
- Focus management
- Color contrast (WCAG)
- Alt text for images

**Animations:**
- CSS Transitions
- **Framer Motion** (recommended):
  ```javascript
  <motion.div
    initial={{ opacity: 0 }}
    animate={{ opacity: 1 }}
    transition={{ duration: 0.5 }}
  />
  ```
- React Spring (physics-based)
- GSAP (advanced)

**📦 Projects Tháng 7-8:**
1. **Portfolio Redesign** - Tailwind, animations, responsive
2. **Admin Dashboard** - MUI/Ant Design, charts, tables
3. **Landing Page Modern** - Framer Motion, parallax, hero sections
4. **Component Library** - Storybook, reusable components
5. **E-commerce UI** - Product cards, filters, checkout flow
6. **Dark Mode System** - Theme switcher, persistence

**🎯 Checkpoint Tháng 8:**
- [ ] Thành thạo Tailwind CSS
- [ ] Hiểu component libraries
- [ ] Animations với Framer Motion
- [ ] Responsive design patterns
- [ ] Accessibility basics
- [ ] 18 projects total

---

### **THÁNG 9-10: React Router & Advanced Navigation**

#### **Week 21-22: React Router v6 Complete**

**Basic Routing:**
- BrowserRouter vs HashRouter
- Routes & Route
- Link & NavLink (active styling)
- Navigate component (redirect)
- Outlet (nested routes)

**Dynamic Routing:**
- URL parameters (:id, :slug)
- useParams Hook
- Optional parameters (:id?:)
- Catch-all routes (*))
- Query strings
- useSearchParams Hook

**Advanced Patterns:**
- **Nested Routes:**
  ```javascript
  <Route path="dashboard" element={<Dashboard />}>
    <Route path="profile" element={<Profile />} />
    <Route path="settings" element={<Settings />} />
  </Route>
  ```

- **Protected Routes:**
  ```javascript
  const ProtectedRoute = ({ children }) => {
    const isAuth = useAuth();
    return isAuth ? children : <Navigate to="/login" />;
  };
  ```

- **Route Guards:**
  - Authentication checks
  - Authorization (role-based)
  - Redirect logic

**Code Splitting & Lazy Loading:**
```javascript
const About = lazy(() => import('./About'));

<Suspense fallback={<Loading />}>
  <Route path="/about" element={<About />} />
</Suspense>
```

**Navigation Patterns:**
- Programmatic navigation (useNavigate)
- Navigation state (passing data)
- useLocation Hook
- useNavigate with replace
- Scroll restoration
- 404 pages

**📦 Projects Tháng 9-10:**
1. **Blog Platform** - Dynamic routes, slug-based URLs
2. **Multi-page Portfolio** - Nested routes, smooth transitions
3. **Auth System UI** - Protected routes, login/logout flow
4. **Documentation Site** - Sidebar navigation, nested sections
5. **E-commerce Router** - Products, Categories, Cart, Checkout

**🎯 Checkpoint Tháng 10:**
- [ ] React Router v6 mastery
- [ ] Protected routes pattern
- [ ] Code splitting với lazy
- [ ] Navigation best practices
- [ ] 23 projects total

---

### **THÁNG 11-12: State Management Ecosystem**

#### **Week 23-24: Context API Advanced Patterns**

**Context Optimization:**
- Multiple contexts strategy
- Provider composition
- Splitting contexts (avoid re-renders)
- Context selector pattern
- Memoization with Context

**Context + Reducer Pattern:**
```javascript
const StateContext = createContext();
const DispatchContext = createContext();

const reducer = (state, action) => {
  switch(action.type) {
    case 'ADD': return [...state, action.payload];
    default: return state;
  }
};

const Provider = ({ children }) => {
  const [state, dispatch] = useReducer(reducer, initialState);
  return (
    <StateContext.Provider value={state}>
      <DispatchContext.Provider value={dispatch}>
        {children}
      </DispatchContext.Provider>
    </StateContext.Provider>
  );
};
```

---

#### **Week 25-26: Redux Toolkit (Modern Redux)**

**Redux Toolkit Setup:**
- Installation (@reduxjs/toolkit, react-redux)
- configureStore
- Creating slices (createSlice)
- Actions & Reducers auto-generated
- Immer integration (mutable updates)

**Redux Core Concepts:**
- Store (single source of truth)
- Actions (plain objects)
- Reducers (pure functions)
- Dispatch
- Selectors

**Redux DevTools:**
- Time-travel debugging
- Action history
- State inspection
- Middleware visualization

**Async Actions:**
- **Redux Thunk** (built-in):
  ```javascript
  const fetchUser = createAsyncThunk(
    'users/fetch',
    async (userId) => {
      const response = await api.getUser(userId);
      return response.data;
    }
  );
  ```

- Handling loading states
- Error handling
- Optimistic updates

**RTK Query (Introduction):**
- API slice creation
- Auto-generated hooks
- Caching strategies
- Invalidation tags

**Redux Best Practices:**
- Normalized state shape
- Selectors with Reselect
- State immutability
- Action naming conventions
- Folder structure (features)

---

#### **Week 27-28: Alternative State Solutions**

**Zustand** (Lightweight):
```javascript
import create from 'zustand';

const useStore = create((set) => ({
  count: 0,
  increment: () => set((state) => ({ count: state.count + 1 }))
}));

// Usage
const count = useStore(state => state.count);
const increment = useStore(state => state.increment);
```
- Minimal boilerplate
- No providers needed
- Middleware (persist, devtools)
- Shallow comparison

**Jotai** (Atomic State):
- Atom-based state
- Bottom-up approach
- No string keys
- TypeScript-friendly

**Recoil** (Facebook):
- Atoms & Selectors
- Derived state
- Async selectors
- Atoms effects

**State Management Decision Tree:**
- **Local state** (useState): Component-specific
- **Shared state** (Context): 2-3 components
- **Global state** (Redux/Zustand): App-wide, complex logic
- **Server state** (React Query): API data

**📦 Projects Tháng 11-12:**
1. **Shopping Cart Full** - Redux Toolkit, RTK Query
2. **Social Media Dashboard** - Complex state, normalized data
3. **Kanban Board** - Zustand, drag & drop, persistence
4. **Real-time Chat UI** - State synchronization patterns
5. **Multi-user Todo** - Shared state, optimistic updates

**🎯 Checkpoint Tháng 12 (End of Year 1):**
- [ ] Redux Toolkit proficiency
- [ ] Understand state management tradeoffs
- [ ] Can choose right tool for job
- [ ] 28 projects completed
- [ ] Ready for production apps
- [ ] GitHub: 200+ commits, clean profile

---

## 🔥 PHASE 3: ADVANCED DEVELOPMENT (Tháng 13-18)

> **Mục tiêu**: TypeScript, Testing, Performance, Next.js

---

### **THÁNG 13-14: Data Fetching & API Integration**

#### **Week 29-30: HTTP Clients & REST APIs**

**Axios Advanced:**
- Interceptors (request, response)
- Default configurations
- Request cancellation (AbortController)
- Retry logic
- Timeout handling
- Axios instances

**Fetch API Patterns:**
- Wrapper functions
- Error handling
- Response types (json, blob, text)
- Headers & CORS
- Credentials (cookies)

**API Integration Patterns:**
- CRUD operations
- RESTful endpoints
- Authentication (JWT tokens)
- Refresh token flow
- File uploads (FormData)
- Progress tracking
- Pagination (offset, cursor)
- Filtering & Sorting
- Search implementation

---

#### **Week 31-32: React Query / TanStack Query** ⭐

**Core Concepts:**
```javascript
const { data, isLoading, error } = useQuery({
  queryKey: ['todos'],
  queryFn: fetchTodos,
  staleTime: 5000,
  cacheTime: 10000,
});
```

- Query keys (arrays, objects)
- Query functions
- Automatic refetching
- Background updates
- Window focus refetching

**Mutations:**
```javascript
const mutation = useMutation({
  mutationFn: addTodo,
  onSuccess: () => {
    queryClient.invalidateQueries(['todos']);
  },
});
```

**Advanced Patterns:**
- Query invalidation
- Optimistic updates
- Infinite queries (useInfiniteQuery)
- Prefetching (queryClient.prefetchQuery)
- Dependent queries (enabled option)
- Parallel queries
- Query cancellation

**Caching Strategies:**
- staleTime vs cacheTime
- Cache persistence (localStorage)
- Cache busting
- Selective refetching

**Error Handling:**
- Global error handling
- Retry configuration
- Error boundaries integration
- Fallback UI

**SWR** (Alternative - Comparison):
- Stale-While-Revalidate
- Focus revalidation
- Simpler API
- When to choose SWR vs React Query

**📦 Projects Tháng 13-14:**
1. **Full CRUD Dashboard** - React Query, all patterns
2. **Infinite Image Gallery** - useInfiniteQuery, Unsplash API
3. **Analytics Dashboard** - Multiple endpoints, parallel fetching
4. **Advanced Search System** - Debouncing, caching, filters
5. **Social Media Feed** - Optimistic updates, pagination

**🎯 Checkpoint Tháng 14:**
- [ ] React Query mastery
- [ ] Understand caching strategies
- [ ] Optimistic updates pattern
- [ ] API integration expert
- [ ] 33 projects total

---

### **THÁNG 15-16: TypeScript Integration**

#### **Week 33-34: TypeScript Fundamentals for React**

**TypeScript Basics:**
- Type annotations
- Interfaces vs Types
- Union types (string | number)
- Intersection types (A & B)
- Type aliases
- Literal types
- any, unknown, never
- Type assertions (as)
- Type guards (typeof, instanceof)

**TypeScript Advanced:**
- Generics
  ```typescript
  function identity<T>(arg: T): T {
    return arg;
  }
  ```
- Utility types:
  - Partial\<T\>
  - Required\<T\>
  - Pick\<T, K\>
  - Omit\<T, K\>
  - Record\<K, V\>
  - Exclude\<T, U\>
  - Extract\<T, U\>
  - ReturnType\<T\>

---

#### **Week 35-36: React with TypeScript**

**Component Typing:**
```typescript
interface Props {
  name: string;
  age?: number; // optional
  onClick: (id: string) => void;
  children: React.ReactNode;
}

const Component: React.FC<Props> = ({ name, age, onClick, children }) => {
  // ...
};

// Or (preferred):
const Component = ({ name, age, onClick, children }: Props) => {
  // ...
};
```

**Hooks Typing:**
```typescript
// useState
const [count, setCount] = useState<number>(0);
const [user, setUser] = useState<User | null>(null);

// useEffect
useEffect(() => {
  // TypeScript infers return type
}, [dependency]);

// useRef
const inputRef = useRef<HTMLInputElement>(null);

// useReducer
const [state, dispatch] = useReducer<Reducer<State, Action>>(reducer, initialState);

// Custom hooks
function useLocalStorage<T>(key: string, initialValue: T): [T, (value: T) => void] {
  // ...
}
```

**Event Typing:**
```typescript
const handleClick = (e: React.MouseEvent<HTMLButtonElement>) => {};
const handleChange = (e: React.ChangeEvent<HTMLInputElement>) => {};
const handleSubmit = (e: React.FormEvent<HTMLFormElement>) => {};
```

**Children & Props:**
```typescript
type Props = {
  children: React.ReactNode; // any renderable
  // or
  children: React.ReactElement; // single element
  // or
  children: (data: T) => React.ReactNode; // render prop
};
```

**Generic Components:**
```typescript
interface ListProps<T> {
  items: T[];
  renderItem: (item: T) => React.ReactNode;
}

function List<T>({ items, renderItem }: ListProps<T>) {
  return <>{items.map(renderItem)}</>;
}
```

**Third-party Library Types:**
- @types packages (DefinitelyTyped)
- Typing Redux (@reduxjs/toolkit types)
- Typing React Query
- Typing React Router
- Creating .d.ts files

**TypeScript Best Practices:**
- Avoid `any` (use `unknown`)
- Use strict mode (tsconfig.json)
- Type everything (no implicit any)
- DRY with type reuse
- Discriminated unions for state

**📦 Projects Tháng 15-16:**
1. **Convert 5 JavaScript projects to TypeScript**
2. **Type-safe API Client** - Axios + TypeScript
3. **Generic Data Table** - Fully typed, reusable
4. **Form Builder** - Type-safe validation
5. **Component Library TS** - Exported types

**🎯 Checkpoint Tháng 16:**
- [ ] TypeScript proficiency
- [ ] Type React components properly
- [ ] Generic components
- [ ] Third-party types
- [ ] 38 projects total

---

### **THÁNG 17-18: Testing Mastery**

#### **Week 37-38: Jest & React Testing Library**

**Jest Fundamentals:**
- Test structure (describe, it, test)
- Matchers:
  - expect(value).toBe()
  - toEqual(), toMatch(), toContain()
  - toBeTruthy(), toBeFalsy()
  - toThrow(), toHaveBeenCalled()
- Setup & Teardown (beforeEach, afterEach)
- Mocking functions (jest.fn())
- Mocking modules (jest.mock())
- Async testing (async/await)
- Snapshot testing

**React Testing Library** ⭐:
```javascript
import { render, screen, fireEvent, waitFor } from '@testing-library/react';
import userEvent from '@testing-library/user-event';

test('counter increments', async () => {
  render(<Counter />);
  const button = screen.getByRole('button', { name: /increment/i });
  await userEvent.click(button);
  expect(screen.getByText(/count: 1/i)).toBeInTheDocument();
});
```

**RTL Philosophy:**
- Test behavior, not implementation
- Query priority:
  1. getByRole (accessibility)
  2. getByLabelText (forms)
  3. getByPlaceholderText
  4. getByText
  5. getByTestId (last resort)

**Query Variants:**
- getBy* - throws error if not found
- queryBy* - returns null
- findBy* - async, waits

**User Interactions:**
- fireEvent vs userEvent (prefer userEvent)
- Typing: userEvent.type()
- Clicking: userEvent.click()
- Keyboard: userEvent.keyboard('{Enter}')

---

#### **Week 39-40: Advanced Testing Patterns**

**Testing Hooks:**
```javascript
import { renderHook, act } from '@testing-library/react';

test('useCounter', () => {
  const { result } = renderHook(() => useCounter());
  
  act(() => {
    result.current.increment();
  });
  
  expect(result.current.count).toBe(1);
});
```

**Testing Context:**
```javascript
const wrapper = ({ children }) => (
  <ThemeProvider>{children}</ThemeProvider>
);

render(<Component />, { wrapper });
```

**Mocking APIs (MSW - Mock Service Worker):**
```javascript
import { rest } from 'msw';
import { setupServer } from 'msw/node';

const server = setupServer(
  rest.get('/api/user', (req, res, ctx) => {
    return res(ctx.json({ name: 'John' }));
  })
);

beforeAll(() => server.listen());
afterEach(() => server.resetHandlers());
afterAll(() => server.close());
```

**Testing Redux:**
- Store creation for tests
- Dispatch actions
- Test selectors
- Test reducers in isolation

**Testing React Query:**
- QueryClientProvider wrapper
- waitFor data fetching
- Test mutations

**Testing Router:**
- MemoryRouter for tests
- Testing navigation
- Testing protected routes

**Integration Testing:**
- Testing full user flows
- Multiple components
- Real API calls (test environment)

**Coverage:**
```bash
npm test -- --coverage
```
- Statement coverage
- Branch coverage
- Function coverage
- Line coverage
- Aim for 80%+ critical paths

---

#### **Week 41-42: E2E Testing (Cypress)**

**Cypress Setup:**
```javascript
describe('Todo App', () => {
  beforeEach(() => {
    cy.visit('/');
  });

  it('adds a new todo', () => {
    cy.get('[data-testid="input"]').type('New todo{enter}');
    cy.contains('New todo').should('exist');
  });
});
```

**Cypress Commands:**
- cy.get(), cy.contains()
- cy.click(), cy.type()
- cy.visit(), cy.intercept()
- cy.wait()
- Custom commands

**API Mocking:**
```javascript
cy.intercept('GET', '/api/todos', { fixture: 'todos.json' });
```

**Playwright** (Alternative):
- Multi-browser testing (Chromium, Firefox, WebKit)
- Parallel execution
- Auto-waiting
- Modern API

**Visual Regression:**
- Percy (visual testing)
- Chromatic (Storybook)

**📦 Projects Tháng 17-18:**
1. **Add tests to 5 existing projects** (80%+ coverage
---

### **THÁNG 19-20: Performance Optimization**

#### **Week 43-44: React Performance Fundamentals**

**React Rendering Behavior:**
- Reconciliation algorithm
- Diffing algorithm
- Keys importance
- Component re-rendering triggers

**React DevTools Profiler:**
- Flame graph analysis
- Ranked chart
- Component render time
- Identifying bottlenecks

**Memoization Techniques:**
- React.memo (when & why)
- useMemo (expensive calculations)
- useCallback (stable functions)
- Memoization anti-patterns

**List Optimization:**
- Windowing (react-window, react-virtual)
- Infinite scroll optimization
- Pagination strategies

---

#### **Week 45-46: Advanced Performance**

**Bundle Optimization:**
- Tree shaking
- Dead code elimination
- Webpack/Vite optimization
- Analyzing bundle size (webpack-bundle-analyzer)
- Dynamic imports
- Preloading & Prefetching

**Image Optimization:**
- Lazy loading
- WebP format
- Responsive images
- CDN setup

**Web Vitals:**
- LCP (Largest Contentful Paint)
- FID (First Input Delay)
- CLS (Cumulative Layout Shift)
- Measuring & improving

**Performance Best Practices:**
- Avoid inline functions
- Avoid inline objects
- Debouncing & Throttling
- Web Workers (heavy computation)
- Service Workers (caching)

**📦 Projects Tháng 19-20:**
1. **Optimize 5 existing projects** (Lighthouse 90+)
2. **Virtual Scrolling Table** (10k+ rows)
3. **Image Gallery Optimized** (lazy loading, WebP)
4. **Performance Dashboard** (track Core Web Vitals)

**🎯 Checkpoint Tháng 20:**
- [ ] React DevTools Profiler mastery
- [ ] Bundle analysis & optimization
- [ ] Web Vitals optimization
- [ ] 43 projects total

---

### **THÁNG 21-22: Next.js Mastery**

#### **Week 47-48: Next.js 14+ Fundamentals**

**App Router vs Pages Router:**
- File-based routing
- Layouts & Templates
- Loading UI
- Error handling
- Not Found pages

**Rendering Strategies:**
- Server Components (RSC)
- Client Components ('use client')
- Static Generation (SSG)
- Server-Side Rendering (SSR)
- Incremental Static Regeneration (ISR)
- Khi nào dùng strategy nào?

**Data Fetching:**
- fetch with caching
- Server Actions
- Streaming & Suspense
- Parallel data fetching
- Sequential data fetching

---

#### **Week 49-50: Advanced Next.js**

**Middleware:**
- Route protection
- Internationalization
- A/B testing
- Custom headers

**API Routes:**
- Route handlers (App Router)
- Serverless functions
- API authentication
- File uploads

**Optimization:**
- Image optimization (next/image)
- Font optimization (next/font)
- Script optimization (next/script)
- Metadata API (SEO)
- Analytics

**Deployment:**
- Vercel deployment
- Environment variables
- Preview deployments
- Production builds

**React 18/19 Features:**
- Concurrent rendering
- Automatic batching
- Transitions (useTransition)
- useDeferredValue
- Server Components deep dive

**📦 Projects Tháng 21-22:**
1. **Blog Platform** (App Router, MDX)
2. **E-commerce Store** (SSG + ISR)
3. **Dashboard** (Server Components + Client)
4. **SaaS Landing Page** (SEO optimized)

**🎯 Checkpoint Tháng 22:**
- [ ] Next.js 14 App Router mastery
- [ ] Server Components vs Client Components
- [ ] ISR/SSG/SSR patterns
- [ ] 48 projects total

---

### **THÁNG 23-24: Full-Stack & Production Deployment**

#### **Week 51-52: Full-Stack Integration**

**Database Integration:**
- Prisma ORM
- PostgreSQL setup
- Schema design
- Migrations
- Database relationships

**Authentication:**
- NextAuth.js
- OAuth providers (Google, GitHub)
- JWT tokens
- Protected routes
- Session management

**File Uploads:**
- Cloud storage (AWS S3, Cloudinary)
- Image processing
- File validation
- Progress tracking

---

#### **Week 53-54: Deployment & DevOps**

**Build Tools:**
- Webpack advanced config
- Vite optimization
- Docker containerization
- CI/CD pipelines (GitHub Actions)

**Cloud Deployment:**
- Vercel (recommended)
- Netlify
- AWS Amplify
- Environment management

**Security:**
- HTTPS setup
- CORS configuration
- XSS protection
- CSRF protection
- Security headers

**Monitoring:**
- Error tracking (Sentry)
- Analytics (Google Analytics)
- Performance monitoring
- Uptime monitoring

**📦 Projects Tháng 23-24:**
1. **Full-stack E-commerce** (Next.js + Database + Auth)
2. **SaaS Application** (payments, subscriptions)
3. **Production Deployment Pipeline** (CI/CD)
4. **Capstone Project** (all skills combined)

**🎯 Checkpoint Tháng 24 (Graduation):**
- [ ] Full-stack production app
- [ ] CI/CD pipeline
- [ ] Security best practices
- [ ] 52+ projects completed
- [ ] Senior React Developer ready

---

## 📚 TÀI LIỆU & TÀI NGUYÊN

### **Documentation (Ưu tiên số 1)**
- [React.dev](https://react.dev) - Official docs
- [Next.js Docs](https://nextjs.org/docs)
- [TypeScript Handbook](https://www.typescriptlang.org/docs/)
- [Testing Library](https://testing-library.com/docs/react-testing-library/intro/)

### **Sách Nên Đọc**
- "Learning React" - Alex Banks & Eve Porcello
- "Fullstack React" - Anthony Accomazzo
- "React Design Patterns and Best Practices"

### **Khóa Học Video**
- freeCodeCamp React (miễn phí)
- Scrimba React (interactive)
- Udemy: "React - The Complete Guide"

### **Communities**
- Reactiflux Discord, Reddit r/reactjs
- Dev.to React Tag, Stack Overflow

---

## 📅 LỊCH HỌC ĐỀ XUẤT

### **Hàng ngày**
- **Sáng (2h)**: Lý thuyết + video
- **Chiều (2-3h)**: Code tutorial + thực hành
- **Tối (1-2h)**: Project cá nhân

### **Hàng tuần**
- Code 5-6 ngày (15-20h)
- Đọc docs 3-4h
- Xem videos 2-3h
- Build projects 10-15h
- Push GitHub hàng ngày

### **Hàng tháng**
- 2-3 projects hoàn thành
- Review & refactor code cũ
- 1 blog post
- Tham gia 1-2 discussions

---

## 💼 PORTFOLIO PROJECTS (52+ Projects)

### **Foundation (Tháng 1-6)**
1-12. Portfolio, Todo, Weather, Calculator, Quiz, Recipe Finder, etc.

### **Intermediate (Tháng 7-12)**
13-28. Styling projects, Routing, State management

### **Advanced (Tháng 13-20)**
29-43. API integration, TypeScript, Testing, Performance

### **Capstone (Tháng 21-24)**
44-52. Next.js, Full-stack, Production apps

---

## 🎓 SELF-ASSESSMENT CHECKLIST

### **Sau 6 tháng - React Junior**
- [ ] JavaScript ES6+ mastery
- [ ] React fundamentals
- [ ] 12 projects completed

### **Sau 12 tháng - React Mid-level**
- [ ] Styling, Routing, State management
- [ ] 28 projects, production ready

### **Sau 18 tháng - React Senior (Junior)**
- [ ] TypeScript, Testing, Performance
- [ ] 43 projects, advanced patterns

### **Sau 24 tháng - React Senior**
- [ ] Next.js, Full-stack, Production
- [ ] 52+ projects, leadership ready

---

## ⚠️ LƯU Ý QUAN TRỌNG

### **❌ Tránh sai lầm phổ biến**
1. Tutorial hell - Code ngay!
2. Học nhiều cùng lúc - Focus 1 concept
3. Không làm projects - Lý thuyết quên nhanh
4. Copy-paste - Hiểu từng dòng
5. Bỏ qua JS fundamentals

### **✅ Thói quen tốt**
1. Code mỗi ngày
2. Đọc code người khác
3. Viết clean code
4. Comment logic phức tạp
5. Google hiệu quả
6. Ask for help
7. Build in public
8. Stay updated

---

## 🚀 BƯỚC TIẾP THEO SAU 2 NĂM

### **Chuyên sâu hơn**
- React Native - Mobile development
- Micro-frontends - Large-scale architecture
- Web3/Blockchain - DApps development
- AI/ML Integration - OpenAI, TensorFlow.js

### **Mở rộng Stack**
- Backend: Node.js, Express, NestJS
- Databases: PostgreSQL, MongoDB, Redis
- Cloud: AWS, Google Cloud, Azure

---

**Version**: 4.0 - Ultimate Combined Roadmap October 2025  
**Happy coding! 🚀**